const aux = (props) => props.children;

export default aux;

// HOC they have on speciality they are not presntational. They wrap other components to add a functionality. Saves redundant root elements. You can return array elements and adjacent html elements.

//HOC can add logic to components